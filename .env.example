# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                     SlideFinder Configuration                              ║
# ║  Copy this file to .env and update values for your environment             ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# ─────────────────────────────────────────────────────────────────────────────
# Application Settings
# ─────────────────────────────────────────────────────────────────────────────
# Application display name (shown in UI and logs)
APP_NAME=SlideFinder

# Enable debug mode (enables hot-reload in development)
DEBUG=false

# ─────────────────────────────────────────────────────────────────────────────
# Server Configuration  
# ─────────────────────────────────────────────────────────────────────────────
# Host to bind the server to (0.0.0.0 for all interfaces)
HOST=0.0.0.0

# Port to run the server on (1-65535)
PORT=7004

# ─────────────────────────────────────────────────────────────────────────────
# Data Storage
# ─────────────────────────────────────────────────────────────────────────────
# Base directory for all data (index, ppts, thumbnails, compiled decks)
# Relative path from workspace root or absolute path
DATA_DIR=data

# ─────────────────────────────────────────────────────────────────────────────
# Azure OpenAI Configuration (Required for AI Deck Builder)
# ─────────────────────────────────────────────────────────────────────────────
# Get these from Azure Portal → Azure OpenAI resource → Keys and Endpoint
# Leave empty/unset to disable AI features

# API key for Azure OpenAI (sensitive - keep secret!)
AZURE_OPENAI_API_KEY=

# Azure OpenAI endpoint URL (e.g., https://your-resource.openai.azure.com/)
AZURE_OPENAI_ENDPOINT=

# Deployment name for chat completions (e.g., gpt-4o, gpt-4o-mini)
AZURE_OPENAI_DEPLOYMENT=gpt-4o

# API version (check Azure docs for latest)
AZURE_OPENAI_API_VERSION=2024-10-21

# ─────────────────────────────────────────────────────────────────────────────
# Search Configuration
# ─────────────────────────────────────────────────────────────────────────────
# Maximum number of slides returned by search API (1-1000)
SEARCH_RESULTS_LIMIT=250

# ─────────────────────────────────────────────────────────────────────────────
# Azure AI Search Configuration (Required for search functionality)
# ─────────────────────────────────────────────────────────────────────────────
# Azure AI Search endpoint URL (e.g., https://your-search.search.windows.net)
AZURE_SEARCH_ENDPOINT=

# Azure AI Search API key (sensitive - keep secret!)
AZURE_SEARCH_API_KEY=

# Azure AI Search index name
AZURE_SEARCH_INDEX_NAME=slidefinder

# ─────────────────────────────────────────────────────────────────────────────
# PPTX Download Configuration
# ─────────────────────────────────────────────────────────────────────────────
# Timeout in seconds for downloading PowerPoint files (10-600)
PPTX_DOWNLOAD_TIMEOUT=120

# ─────────────────────────────────────────────────────────────────────────────
# CORS Configuration
# ─────────────────────────────────────────────────────────────────────────────
# Allowed origins for CORS (comma-separated, or * for all)
# Examples: 
#   CORS_ORIGINS=["http://localhost:3000","https://myapp.com"]
#   CORS_ORIGINS=["*"]
CORS_ORIGINS=["*"]
